package com.example.latte_core.app;

/**
 * 全局枚举类，定义枚举变量
 */
public  enum ConfigKeys {
    API_HOST,
    APPLICATION_CONTEXT,
    CONFIG_READY,
    ICON,
    INTERCEPTOR,
    LOADER_DELAYED,
    HANDLER
}
